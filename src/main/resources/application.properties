# Con valores por defecto para desarrollo local
spring.datasource.url=jdbc:postgresql://${PGHOST:interchange.proxy.rlwy.net}:${PGPORT:18277}/${PGDATABASE:railway}
spring.datasource.username=${PGUSER:postgres}
spring.datasource.password=${PGPASSWORD:bhGIayJJktrswKVCbxzjGbiPlHhgajtQ}

# JPA (para q cree automaticamene las tablas "si no existen")
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# Server
server.port=${PORT:8080}

