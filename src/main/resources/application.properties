# Configuración para Railway
# Intenta usar DATABASE_URL primero, si no usa las variables individuales
spring.datasource.url=jdbc:${DATABASE_URL:postgresql://${PGHOST:interchange.proxy.rlwy.net}:${PGPORT:18277}/${PGDATABASE:railway}}
spring.datasource.username=${PGUSER:postgres}
spring.datasource.password=${PGPASSWORD:bhGIayJJktrswKVCbxzjGbiPlHhgajtQ}

# Configuración Spring
spring.application.name=siiga2.0-web
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server
server.address=0.0.0.0
server.port=${PORT:8095}